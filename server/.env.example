# Copy this file to .env and adjust the values per environment

# Spring profile
SPRING_PROFILES_ACTIVE=dev

# Postgres container configuration (used by docker-compose)
POSTGRES_DB=area
POSTGRES_USER=area_admin
POSTGRES_PASSWORD=change-me

# Datasource configuration for Spring Boot.
# By default, application.properties will connect to:
#   jdbc:postgresql://localhost:8088/${POSTGRES_DB}
# using POSTGRES_USER / POSTGRES_PASSWORD.
# You can override these with the SPRING_DATASOURCE_* variables if needed.
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:8088/${POSTGRES_DB}
SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}

# JWT secret placeholder; set a strong, unique value in your local .env
APP_JWT_SECRET=replace-with-real-jwt-secret

# Google OAuth Configuration for Gmail integration
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/oauth/callback

# GitHub OAuth Configuration
# Create OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret
GITHUB_REDIRECT_URI=http://localhost:8080/api/services/github/callback

# Discord Bot Configuration
DISCORD_CLIENT_SECRET=your-discord-bot-token
DISCORD_CHANNEL_ID=your-discord-channel-id
